import Contact from "../components/Contact";

export const metadata = {
    title: "Contact | GKELITE",
    description: "GKELITE-INFO-Contact",
};

const Page = () => {
    return <Contact />;
};

export default Page;
